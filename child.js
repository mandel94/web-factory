/**
 * This class represents a child
 * 
 * Each child has at least (and only) one direct parent, from which inherits all
 *  'genetic' properties from parents.
 * 
 */
class Child {
    /**
     * 
     * @param {Tag} parent - An HTML tag 
     */
    constructor(parent) {
        this.parent = parent;
    }


}